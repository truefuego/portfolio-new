export interface IFilterButtonProps{
    title: string;
    isActive: boolean;
    onClick: () => void;
};

export interface IFilterIconProps {
    Icon: string;
    isActive: boolean;
    onClick: () => void;
};
